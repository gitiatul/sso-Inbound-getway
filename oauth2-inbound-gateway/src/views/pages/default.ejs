<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <% var page_title="SSO SMART Inbound Gateway | KPI Ninja" ; var page_description="KPI Ninja SSO SMART Inbound Gateway" ;
    var
    keywords="KPI Ninja" ; if(typeof data.page_meta !="undefined" && data.page_meta !=null && data.page_meta !="" ){
    page_title=data.page_meta.title; page_description=data.page_meta.description; keywords=data.page_meta.keywords; } %>
    <title>
      <%= page_title %>
    </title>
    <meta name="description" content="<%= page_description %>" />
    <meta name="keywords" content="<%= keywords %>" />
    <!-- Bootstrap CSS 4.5.3 -->
    <link rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.5.3/css/bootstrap.min.css" />
    <!-- Font Awesome CSS 5.15.3 -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" />
    <style>
      * {
        -webkit-box-sizing: border-box;
        box-sizing: border-box;
      }

      body {
        font-family: "Kanit", sans-serif;
        padding: 0;
        margin: 0;
        /* padding-top: 50px; */
      }

      .brand-logo {
        width: auto;
        height: 45px;
      }

      #main {
        position: relative;
        height: 100vh;
      }

      #main .main {
        position: absolute;
        left: 50%;
        top: 50%;
        -webkit-transform: translate(-50%, -50%);
        -ms-transform: translate(-50%, -50%);
        transform: translate(-50%, -50%);
      }

      .main {
        max-width: 767px;
        width: 100%;
        line-height: 1.4;
        text-align: center;
        padding: 15px;
      }

      .main .statuscode {
        position: relative;
        height: 220px;
      }

      .main .statuscode h1 {
        font-family: "Kanit", sans-serif;
        position: absolute;
        left: 50%;
        top: 50%;
        -webkit-transform: translate(-50%, -50%);
        -ms-transform: translate(-50%, -50%);
        transform: translate(-50%, -50%);
        font-size: 186px;
        font-weight: 200;
        margin: 0px;
        background: linear-gradient(130deg, #00A9FC, #B163FB, #5D48FB);
        color: transparent;
        -webkit-background-clip: text;
        background-clip: text;
        text-transform: uppercase;
      }

      .main h2 {
        font-family: "Kanit", sans-serif;
        font-size: 33px;
        font-weight: 200;
        text-transform: uppercase;
        margin-top: 0px;
        margin-bottom: 25px;
        letter-spacing: 3px;
      }

      .main p {
        font-family: "Kanit", sans-serif;
        font-size: 16px;
        font-weight: 200;
        margin-top: 0px;
        margin-bottom: 25px;
      }

      .main a {
        font-family: "Kanit", sans-serif;
        color: #ff6f68;
        font-weight: 200;
        text-decoration: none;
        border-bottom: 1px dashed #ff6f68;
        border-radius: 2px;
      }

      .main-social>a {
        display: inline-block;
        height: 40px;
        line-height: 40px;
        width: 40px;
        font-size: 14px;
        color: #ff6f68;
        border: 1px solid #efefef;
        border-radius: 50%;
        margin: 3px;
        -webkit-transition: 0.2s all;
        transition: 0.2s all;
      }

      .main-social>a:hover {
        color: #fff;
        background-color: #ff6f68;
        border-color: #ff6f68;
      }

      @media only screen and (max-width: 480px) {
        .main .statuscode {
          position: relative;
          height: 168px;
        }

        .main .statuscode h1 {
          font-size: 142px;
        }

        .main h2 {
          font-size: 22px;
        }
      }
    </style>
    <link rel="apple-touch-icon" sizes="57x57" href="<%= data.images.favicon %>" />
    <link rel="apple-touch-icon" sizes="60x60" href="<%= data.images.favicon %>" />
    <link rel="apple-touch-icon" sizes="72x72" href="<%= data.images.favicon %>" />
    <link rel="apple-touch-icon" sizes="76x76" href="<%= data.images.favicon %>" />
    <link rel="apple-touch-icon" sizes="114x114" href="<%= data.images.favicon %>" />
    <link rel="apple-touch-icon" sizes="120x120" href="<%= data.images.favicon %>" />
    <link rel="apple-touch-icon" sizes="144x144" href="<%= data.images.favicon %>" />
    <link rel="apple-touch-icon" sizes="152x152" href="<%= data.images.favicon %>" />
    <link rel="apple-touch-icon" sizes="180x180" href="<%= data.images.favicon %>" />
    <link rel="icon" type="image/png" sizes="192x192" href="<%= data.images.favicon %>" />
    <link rel="icon" type="image/png" sizes="32x32" href="<%= data.images.favicon %>" />
    <link rel="icon" type="image/png" sizes="96x96" href="<%= data.images.favicon %>" />
    <link rel="icon" type="image/png" sizes="16x16" href="<%= data.images.favicon %>" />
    <meta name="msapplication-TileColor" content="#ffffff" />
    <meta name="msapplication-TileImage" content="<%= data.images.favicon %>" />
    <meta name="theme-color" content="#ffffff" />
</head>

<body>
  <div id="main">
    <div class="main">
      <div>
        <img src="<%= data.images.logo %>" class="brand-logo">
      </div>
      <div class="statuscode">
        <h1>200</h1>
      </div>
      <h5 class="text-success">
        <i class="fas fa-check fa-fw"></i>
        KPI Ninja SSO SMART Inbound Gateway
      </h5>
      <p>Working!!!</p>
      <p class=" text-center text-muted">© Copyright <%= new Date().getFullYear() %> KPI Ninja
      </p>
    </div>
  </div>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.5.3/js/bootstrap.bundle.min.js"></script>
</body>

</html>